# Copy the values from your Supabase project (Settings -> API) and set
# these as environment variables in Netlify (Site settings -> Environment
# variables). Never commit real values to git.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
